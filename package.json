{
  "name": "wvc-editor",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm --workspace client run dev\" \"npm --workspace server run dev\"",
    "build": "npm --workspace client run build && npm --workspace server run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
